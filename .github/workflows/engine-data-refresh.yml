name: Engine data refresh (streams + détection de courses)

# ALIMENTE le moteur. Deux jobs de maintenance, dans cet ordre — qui n'est PAS anodin :
#   1. detect-races     — qualifie les compétitions de l'historique. Ne lit que les
#                         résumés d'activité (FC moyenne, temps, titre) : aucun tracé
#                         requis, donc rien ne l'oblige à attendre le rattrapage.
#   2. backfill-streams — met en cache les tracés GPS manquants. Il PRIORISE les
#                         compétitions confirmées à l'étape 1, y compris hors fenêtre
#                         glissante — d'où l'ordre : détecter d'abord, pour savoir quoi
#                         rattraper en priorité.
#
# POURQUOI CE WORKFLOW EXISTE : `backfill-streams` a été écrit pour être appelé en boucle
# par un cron (~15 min) — mais RIEN ne l'appelait. Vérifié sur la base `runnerdata` le
# 2026-07-30 : ni `pg_cron` ni `pg_net` ne sont installés. Le cache des tracés ne se
# maintenait donc que par les visites des athlètes dans l'app, et dérivait dès qu'un
# athlète synchronisait de nouvelles sorties sans ouvrir les écrans qui déclenchent le
# calcul de profil.
#
# (Note : une version antérieure de ce commentaire annonçait deux athlètes à 11 % et 47 %
# de couverture GPS. C'était FAUX — le calcul divisait les tracés par TOUTES les activités,
# vélo et natation compris, au lieu des seules sorties à pied. Sur les sorties à pied, les
# seules qui alimentent le moteur, la couverture était déjà d'environ 97 %, et le premier
# passage réel n'a ajouté que 14 tracés. Ce workflow reste nécessaire — pour empêcher la
# dérive et pour le rattrapage ciblé des courses — mais il ne rattrape pas un retard massif.)
#
# Prérequis (Settings → Secrets and variables → Actions) — les MÊMES que
# `engine-backtest.yml`, rien de neuf à créer :
#   • SUPABASE_SERVICE_ROLE_KEY  (Dashboard Supabase → Project Settings → API)
#   • l'URL est reprise de SUPABASE_URL, sinon VITE_SUPABASE_URL
#
# ⚠ SUPABASE_ACCESS_TOKEN (sbp_…) est un jeton de DÉPLOIEMENT — ce n'est pas la
#   service_role key et il ne convient pas ici.

on:
  schedule:
    # 04:00 et 16:00 UTC. Deux passages par jour suffisent : la fenêtre moteur est de
    # 183 jours et le quota Strava (100 appels / 15 min) borne de toute façon le débit.
    - cron: '0 4,16 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  # Jamais deux passages en parallèle : ils se disputeraient le quota Strava.
  group: engine-data-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Vérifier les secrets
        env:
          URL: ${{ secrets.SUPABASE_URL || secrets.VITE_SUPABASE_URL }}
          KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          if [ -z "$URL" ] || [ -z "$KEY" ]; then
            echo "::error::Secrets manquants (SUPABASE_URL ou VITE_SUPABASE_URL, et SUPABASE_SERVICE_ROLE_KEY)."
            exit 1
          fi

      # Idempotent : n'écrit que les verdicts qui changent. Tourne APRÈS le backfill pour
      # qualifier l'historique le plus complet possible.
      - name: Détection des courses (jusqu'à épuisement)
        env:
          URL: ${{ secrets.SUPABASE_URL || secrets.VITE_SUPABASE_URL }}
          KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -euo pipefail
          for i in $(seq 1 12); do
            body=$(curl -sS -X POST "$URL/functions/v1/detect-races" \
              -H "Authorization: Bearer $KEY" -H 'Content-Type: application/json' -d '{}')
            echo "passage $i : $body"
            remaining=$(printf '%s' "$body" | grep -o '"remaining":[0-9]*' | cut -d: -f2 || echo '')
            if printf '%s' "$body" | grep -q '"error":"Forbidden"'; then
              echo "::error::403 de detect-races : même cause que ci-dessus (secret sans droits service)."
              exit 1
            fi
            if [ -z "$remaining" ]; then
              echo "::error::Réponse inattendue de detect-races (voir ci-dessus)."
              exit 1
            fi
            [ "$remaining" -eq 0 ] && { echo "Détection à jour."; break; }
            sleep 5
          done

      # Boucle jusqu'à `remaining = 0`. S'arrête net sur quota Strava (429) : la fonction
      # renvoie `rateLimited: true` et le reste sera repris au passage suivant — le cache
      # n'est jamais supprimé, un rattrapage partiel n'est donc jamais perdu.
      - name: Backfill des streams (jusqu'à épuisement ou quota)
        env:
          URL: ${{ secrets.SUPABASE_URL || secrets.VITE_SUPABASE_URL }}
          KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -euo pipefail
          for i in $(seq 1 12); do
            body=$(curl -sS -X POST "$URL/functions/v1/backfill-streams" \
              -H "Authorization: Bearer $KEY" -H 'Content-Type: application/json' -d '{}')
            echo "passage $i : $body"
            remaining=$(printf '%s' "$body" | grep -o '"remaining":[0-9]*' | cut -d: -f2 || echo '')
            limited=$(printf '%s' "$body" | grep -o '"rateLimited":true' || true)
            if [ -n "$limited" ]; then
              echo "::notice::Quota Strava atteint — reprise au prochain passage."
              break
            fi
            if printf '%s' "$body" | grep -q '"error":"Forbidden"'; then
              echo "::error::403 de backfill-streams : le secret SUPABASE_SERVICE_ROLE_KEY n'a pas les droits service. Vérifier qu'il s'agit bien d'une clé secrète (Dashboard → Project Settings → API Keys), et non de la clé publishable/anon ni du jeton de déploiement sbp_…"
              exit 1
            fi
            if [ -z "$remaining" ]; then
              echo "::error::Réponse inattendue de backfill-streams (voir ci-dessus)."
              exit 1
            fi
            [ "$remaining" -eq 0 ] && { echo "Cache des streams à jour."; break; }
            sleep 20
          done
