# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/

# Environment files (NEVER commit secrets)
.env
.env.local
.env.*.local

# Turbo
.turbo/

# Test coverage
coverage/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Supabase local
supabase/.temp/
supabase/.branches/

# TypeScript
*.tsbuildinfo

# Container / environment artifacts (anciens artefacts Expo/RN, plus utilisés)
.devcontainer/
app.json
babel.config.js
expo-env.d.ts
start.sh
stores/

# Claude Code internal
.claude/

# Playwright
playwright-report/
test-results/

# Auth storageState (tokens)
playwright/.auth/*.json

# Variables de test (service role key)
.env.test

# Data dumps / exports (ne jamais committer d'archives de données)
*.zip

# Résultats du banc réel (données personnelles réelles : jamais committées)
artifacts/engine-backtest/
# Fixtures de banc contenant des données réelles (coordonnées GPS, etc.)
*.backtest-fixture.json

# Archives de médias achetés (GymVisual…) — jamais dans le dépôt (voir raw-media/README.md)
/*.zip
/*.ZIP
/*.gif
